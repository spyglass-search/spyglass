[package]
name = "migration"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
sea-orm-migration = { version = "0.10" }
entities = { path = "../entities" }
shared = { path = "../shared" }
tantivy = "0.18"
rayon = "1.5"
tar = "0.4"